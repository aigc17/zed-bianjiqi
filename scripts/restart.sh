#!/bin/bash
# ============================================================================
# Zed Workspace Manager - 重启脚本
# 关闭后重新启动
# ============================================================================

SCRIPTS_DIR="$(cd "$(dirname "$0")" && pwd)"

"$SCRIPTS_DIR/stop.sh"
sleep 0.3
"$SCRIPTS_DIR/start.sh"
